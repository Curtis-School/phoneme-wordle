import { PagePlaceholder } from "@/components/ui/PagePlaceholder";

export default function WordlePage() {
  return (
    <PagePlaceholder
      title="Phoneme Wordle"
      intro="Pick a difficulty, then guess the phoneme word using sound tiles. The builder and playable preview land in an upcoming commit."
    />
  );
}
